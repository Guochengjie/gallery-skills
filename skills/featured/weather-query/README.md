# Weather Query

Simple wttr.in-based weather query skill.

## Secret

Set skill secret to weather host URL, for example:

- https://wttr.in

## Example

```json
{
  "location": "London",
  "format": "%l:+%c+%t+%h+%w"
}
```
